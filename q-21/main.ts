//Question---21

// They think of something you could store in a TypeScript Object. Write a program that creates Objects containing these items.

let obj = {
  name: "Zain Shah",
  age: 15,
  city: "Jamshoro",
};

console.log(
  `My name is ${obj.name}, I am ${obj.age} years old and and I live in ${obj.city}`
);
